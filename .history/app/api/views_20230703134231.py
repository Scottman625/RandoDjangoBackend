from django.http.response import HttpResponse, JsonResponse
from django.shortcuts import render
from modelCore.models

# Create your views here.
def Login(request):
    phone = request.POST.get('phone')
    password = request.POST.get('password')
    if 
    return 

